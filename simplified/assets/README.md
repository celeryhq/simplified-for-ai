# Assets

Brand art for the Codex plugin listing (extracted from simplified.com):

- `simplified-logo.png` — full Simplified wordmark+mark logo (190×192).
- `simplified-treadmark-logo.png` — the Simplified mark (942×190), used as the small icon.

Brand colors: amber `#FFAD00` (primary), gray `#43464E` (wordmark).

Referenced by `.codex-plugin/plugin.json` (`composerIcon`, `logo`) and each skill's
`agents/openai.yaml` (`icon_small`, `icon_large`).

> Note: the treadmark PNG is wide (942×190), not square. If OpenAI's listing wants
> a square 256×256 icon, crop/pad the mark before submission.
